---
title: Models and Provider Connections
description: 理解 provider template、Connection、model 和 variant。
---

Devo 用一个目录描述 provider template 及其模型，再用用户覆盖文件记录用户已经配置的
Connection。模型归属于 provider，因此稳定的公开身份是 `provider/model`。

## 三个概念

| 概念 | 含义 | 存储位置 |
| --- | --- | --- |
| Provider template | 内置的 endpoint 默认值、wire API 和模型元数据。 | git 跟踪的 `crates/core/providers.json` |
| Connection | 用户配置的 provider 实例，包含 endpoint 覆盖项和 credential 引用。 | 用户/工作区 `providers.json` |
| Model | provider-facing model id，以及可选的能力和请求元数据。 | 两个文件中 provider 的嵌套 map |

API key 本身存储在用户级 `auth.json`；`providers.json` 只保存 credential id。TUI 中不能删除
provider template；断开 Connection 会删除用户的 provider 覆盖项。

## Provider 和 Model 身份

Provider id 和嵌套 model id 都是 map key。Devo 不再需要额外的 model slug、model name、request
model 或 description 来保持同步：

```json
{
  "model": "deepseek/deepseek-v4-flash",
  "provider": {
    "deepseek": {
      "base_url": "https://api.deepseek.com/anthropic",
      "credential": "deepseek_api_key",
      "wire_api": "anthropic_messages",
      "models": {
        "deepseek-v4-flash": {
          "name": "DeepSeek V4 Flash",
          "context_window": 200000
        }
      }
    }
  }
}
```

嵌套 model key 同时是发送给 provider 的 id，也是 `provider/model` 引用的后半部分；`name`
仅用于显示。

## Model 元数据

Model record 可以描述上下文长度、输出上限、输入模态、reasoning 控制、采样默认值、能力、
价格、发布状态、任意 request/options/headers 和命名 variants。Onboarding 常用表单会先显示
常用字段，高级设置显示其余字段。

根字段 `small_model` 是可选的轻量后台模型选择，例如用于生成 session 标题。它缺失或无效时，
Devo 会先从主模型所在 provider 中选择明显轻量的模型，最后回退到 `model`。

## Wire API

选中的模型使用以下请求协议之一：

| Wire API | 含义 |
| --- | --- |
| `openai_chat_completions` | OpenAI 兼容 Chat Completions API。 |
| `openai_responses` | OpenAI 兼容 Responses API。 |
| `anthropic_messages` | Anthropic 兼容 Messages API。 |

DeepSeek 官方 Anthropic-compatible endpoint 表示为普通 Connection，其 `wire_api` 为
`anthropic_messages`。

## Reasoning 和 Variants

`reasoning_capability` 决定 picker 展示哪些值。通过 `provider/model/variant` 选择模型 variant；
variant 不是另一种模型身份。Variant 的 request、options 和 headers 会在 model/provider 值之后
合并，因此可以支持 OpenCode 风格的 fast/reasoning variants，而不需要复制 provider 或 model。

## 解析

每个 turn 都会解析明确的 `provider/model[/variant]` 引用，检查 Connection 和 model 已启用，
从 `auth.json` 解析 credential，合并请求默认值，再通过模型的 wire API 发送请求。`/model` 和
CLI `--model` 使用同一种引用格式。

如果尚未配置 Connection，请运行：

```bash
devo onboard
```
